{
  "name": "@chirp-city/subgraph",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:matic": "graph deploy --node https://api.thegraph.com/deploy/ holic/chirp-city subgraph-matic.yaml",
    "deploy:mumbai": "graph deploy --node https://api.thegraph.com/deploy/ holic/chirp-city-testnet subgraph-mumbai.yaml"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1",
    "assemblyscript-regex": "^1.6.4"
  }
}
